import CountHeightComponent from '../Component/CountHeightComponent';
import React from 'react'

const CountHeightContainer = (props) => {
    return (
        <CountHeightComponent 
            {...props}
        />
    )
}

export default CountHeightContainer;